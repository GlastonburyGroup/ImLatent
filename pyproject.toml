[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imlatent"
version = "0.1.0"
description = "This repository provides a pipeline for training and evaluating 2D and 3D diffusion autoencoders, traditional autoencoders, and various variational autoencoders for unsupervised latent representation learning from 2D and 3D images, primarily focusing on MRIs. This repository was developed as part of the paper titled Hundreds of cardiac MRI traits derived using 3D diffusion autoencoders share a common genetic architecture and was utilised to learn and infer latent representations from cardiac MRIs (CINE) using a 3D diffusion autoencoder."
readme = "README.md"
requires-python = ">=3.11, <3.15"
license = { text = "Apache-2.0 license" }
authors = [
  { name = "soumick.chatterjee", email = "soumick.chatterjee@fht.org" }
]

# Dependencies 
dependencies = [
    "h5py>=3.14.0,<4.0.0",
    "pandas>=2.3.0,<3.0.0",
    "scikit-learn>=1.7.0,<2.0.0",
    "zarr>=3.0.8,<4.0.0",
    "torch>=2.10.0,<2.11.0",
    "torchvision>=0.25.0,<0.26.0",
    "lightning>=2.5.2,<3.0.0",
    "torchio>=0.20.14,<0.21.0",
    "pytorch-msssim>=1.0.0,<2.0.0",
    "deepspeed>=0.18.9,<0.20.0",
    "pyssim>=0.7.1,<0.8.0",
    "pythae==0.1.1",
    "transformers>=4.52.4,<5.0.0",
    "latte-metrics[pytorch]>=0.0.1a6",
    "scikit-image>=0.26.0,<0.27.0",
    "pytorch-complex>=0.2.0,<0.3.0",
    "async-timeout>=5.0.1,<6.0.0",
    "sewar>=0.4.6,<0.5.0",
    "numpy>=2.3.1,<3.0.0",
    "ipykernel>=6.30.0,<7.0.0",
    "matplotlib>=3.10.3,<4.0.0",
    "wandb>=0.21.0,<0.22.0",
    "tensorboard>=2.20.0,<3.0.0",
    "tokenizers<0.21.4",
    "pytorch-fid>=0.3.0,<0.4.0",
    "lpips>=0.1.4,<0.2.0",
    "timm>=1.0.22,<2.0.0",
    "fvcore>=0.1.5.post20221221,<0.2.0",
    "monai>=1.5.1,<2.0.0",
    "batchgenerators>=0.25.1,<0.26.0",
    "torchinfo>=1.8.0,<2.0.0"
]

[project.optional-dependencies]
mamba = ["mamba-ssm[causal-conv1d]>=2.3.0"]

[tool.uv]
constraint-dependencies = ["torchaudio==2.10.0"]

[tool.uv.extra-build-dependencies]
mamba-ssm     = [{ requirement = "torch", match-runtime = true }]
causal-conv1d = [{ requirement = "torch", match-runtime = true }]

[tool.hatch.build.targets.wheel]
packages = ["Engineering", "Executors"]  # the actual importable packages in this repo
